file(GLOB KCP_HEADER_FILES "./*.h")

file(GLOB KCP_SOURCE_FILES "./*.c")

add_library(kcp STATIC ${KCP_HEADER_FILES} ${KCP_SOURCE_FILES})

#source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${KCP_SOURCE_FILES} ${KCP_HEADER_FILES})

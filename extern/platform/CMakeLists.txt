file(GLOB PLATFORM_HEADER_FILES "./*.h")

file(GLOB PLATFORM_SOURCE_FILES "./*.cpp")


add_library(platform STATIC ${PLATFORM_HEADER_FILES} ${PLATFORM_SOURCE_FILES})

#source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${PLATFORM_SOURCE_FILES} ${PLATFORM_HEADER_FILES})

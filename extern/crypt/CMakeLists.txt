file(GLOB CRYPT_HEADER_FILES "./*.h")

file(GLOB CRYPT_SOURCE_FILES "./*.c")


add_library(crypt STATIC ${CRYPT_HEADER_FILES} ${CRYPT_SOURCE_FILES})

#source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${CRYPT_SOURCE_FILES} ${CRYPT_HEADER_FILES})
